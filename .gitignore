# Python
# Virtualenvs at repo root and under packages (never commit).
.venv/
.venv-test/
.venv_genify_ci/
.venv312/
venv/
# If someone creates a venv inside tests/ by mistake
tests/.venv/
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
.Python
pip-wheel-metadata/
_run_output/

# Node — never commit deps (Databricks: npm ci + build; upload excludes node_modules)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Frontend build output (rebuilt on deploy; workspace upload uses dist/ only)
src/genify/frontend/dist/
src/genify/frontend/.vite/

# Deploy config (environment-specific, not committed)
deploy.config.yaml
deploy.config.local.yaml
deploy.*.local.yaml

# Environment / secrets
.env
.env.local
.env.*
.envrc
.databrickscfg
*.pem
.id_rsa
.id_ed25519

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Testing
.pytest_cache/
**/.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
junit.xml
.tox/
.hypothesis/

# Type checkers
.mypy_cache/
.ruff_cache/

# Temporary
*.tmp
*.bak
*.orig
*.rej

# Python packaging (repo root — frontend dist/ is ignored explicitly above)
/dist/
/build/

# PEP 582 / other local install layouts
__pypackages__/

# Mypy daemon
.dmypy.json

# Cursor IDE — entire directory ignored; shared rules/skills are not in git unless you
# replace this with selective ignores — see docs/public-repo-checklist.md
.cursor/
_run_output/Table_markdown.md
_run_output/table_yaml.yaml
